# JUNCTION / 継ぎ目

白い建物。部屋は同じ間取りの繰り返し。置いてある椅子もロッカーも、どの部屋でも同じ大きさ。
違うのは部屋の方で、扉をくぐるたびに自分の背丈が変わる。
さっきまで越えられなかった柵が、膝の高さで転がっている。

> **扉が繋ぐのは、見えていた景色ではない。**

## すぐ遊ぶ（エンジン不要・Windows）

```
cd Junction/build/game
Game.exe
```

`build/game/` はビルド済み（起動シーンは第1面）。Windows 10/11 + DirectX 12 対応 GPU だけあればいい。

## 操作

| | |
|---|---|
| WASD | 歩く（Shift で少し速く） |
| マウス | 視点 |
| **SPACE** | 跳ぶ。**跳べる高さも距離も大きさで変わる** |
| R | やり直し |
| 1〜8 | 面を直接選ぶ |
| H | 押している間、操作キーを出す |
| ESC | マウス解放（エディタの Stop を押したい時） |

---

## ルール（プレイヤーには見せない）

契約書は **`docs/V8.md`**。**ここが心臓なので、触る前に必ず読むこと。**（v7 以前は履歴）

1. 部屋には縮尺（0.5 / 1 / 2）がある。壁・天井・扉・目地だけがその倍率。中に居る限り普通の部屋。
2. ★**什器（ベンチ 0.95m / ロッカー 1.95m / 木箱 0.75m）は絶対寸法**。部屋の縮尺を掛けない。
   これが「自分が大きくなった」を目に見えるようにする唯一の装置。
3. 縮尺が無い物は **柵 1.7m / 隙間 1.0m / 溝 4.6m / 高い敷居 0.9m / 出口の門** と什器。これが謎解き。
   通れる縮尺は物理から一意に決まる: 柵と溝は **k=2 だけ**、隙間は **k=0.5 だけ**、
   高い敷居は **k=0.5 の部屋からは登れない = 一方通行**。
4. 緑の門（濃い炭色の枠 + 光る緑の敷居 + 下向き矢印の標識）に触れたらクリア。落ちても戻るだけ。失敗も制限時間も詰みも無い。

| 縮尺 | 自分の背 | ベンチ 0.95m は | ロッカー 1.95m は | 柵 1.7m は |
|---|---|---|---|---|
| 0.5 | 0.90 m | 目の高さ | 見上げる壁 | 越えられない |
| 1 | 1.80 m | 腰 | 自分より高い | 越えられない |
| 2 | 3.60 m | 脛 | 腰 | **膝。跨げる** |

---

## ★2026-09-03 — v8「つなぎ目を抜けて見せ、8 面を錯覚で組み直した」

v7 で錯覚そのものは成立した（第9面）。v8 はそれを **第1面に据え直し、残り 7 面を同じ原理で新規に作り直した**。
旧 1〜8 面（v6 の縮尺だけ違う面）は全廃。

直した 4 点（指摘そのまま → 対処）:

| 指摘 | 真因（実測） | 対処 |
|---|---|---|
| つなぎ目が壁に見えて初見で進めない | 扉板が 70 度で**開口の前に立っていた** + 廊下の中が壁と同じ白さ | 扉板を **105 度（全開）**へ / 廊下に弱い灯り 2 灯 |
| ゴールがゴールに見えない | 白い入れ子の枠は白い部屋では**ただの彫刻** | **非常口の門**へ作り直し（炭色の枠・光る緑の敷居・下向き矢印の標識・床の矢羽根） |
| 照明が変わったことに気づいてしまう | 部屋の灯りを**全部同時に**差し替えていた + 点光源が壁を透ける | **1 灯ずつ、その灯りが背中側にある間だけ** 3 秒かけて混ぜる |
| 錯覚しか無くて遊びが薄い | 関門が縮尺と結びついていなかった | 関門を 4 つに整理し、**通れる縮尺を物理から一意に**決めた |

★ 扉板を全開にしたら、板の裏面が天井の点光源と平行になって**真っ黒**になった。
点光源は `castShadows=false` なので、**環境光を 0.035 → 0.16** に上げて面が読めるようにしてある。

### 8 面

| 面 | 題 | 教える事 | 縮尺の道筋 |
|---|---|---|---|
| 1 | 扉 | 道具一式（偽の廊下・背後改変・什器が物差し） | 1 → 0.5 → 2 |
| 2 | 窓 | 窓は物差し。二択を家具の見え方で選ぶ | 1 → 2 |
| 3 | 敷居 | 小さくなると損をする。膝の敷居が胸になる | 1 → 0.5 |
| 4 | 三つの廊下 | 見えている物を疑う。塞がれた扉だけが本物 | 1 → 2 |
| 5 | 溝 | 柱の太さは変わらない。大広間で柱が爪楊枝になる | 1 → 2 → 0.5 |
| 6 | 輪 | 回ると「最初の部屋」に戻る（実は同じ絵の別室） | 1 → 2 → 1 → 0.5 |
| 7 | 一棟 | 関門を 1 本に繋ぐ。同じ柵が壁/膝/壁/膝に見える | 1 → 2 → 0.5 → 2 |
| 8 | 鏡 | 左右反転した写し。体が覚えた「柵は西」が通用しない | 1 → 2 → 0.5 → 2 |

詳細と**設計の罠**は `docs/V8.md`。★特に「餌（偽の廊下）を柵の向こう側に置く」事故は 4 面で踏んだ。
`check_fakes` が検査するようになっている。

---

## 作り方

```
python source/gen_textures.py          # tex/*.png
python source/gen_sfx.py               # audio/**
python source/gen_stages.py            # scenes/*.json, Junction.lua の STAGES, models/manifest.json(壁・床・廊下)
# 壁モデルが足りないと言われたら BlenderMCP から:
#   JX_MANIFEST_ONLY = True
#   exec(open(r"...source/blender_kit.py", encoding="utf-8").read())
```

`gen_stages.py` はステージを書き出す前に **全状態 (部屋, 区画, 大きさ) を総当たり**して、
出口に着けるか / 想定手数（`minHops`）より短く解けないか / **到達できる全状態から出口へ戻れるか（詰み無し）** /
什器が扉の助走を潰していないか / 什器が柵の踏み台にならないか（★縮尺込みで判定）/
**偽の廊下が到達できる区画に付いているか** を検査する。案内の光の経路（`hint`）もこの最短手順から出る。

`gen_stages.py` は `Junction.lua` の `>>>STAGES`〜`<<<STAGES` を書き換える。配布ビルドは MCP の `dx12_build_game`。

| 担当 | 触ってよい | 触ってはいけない |
|---|---|---|
| モデル | `source/blender_kit.py`, `assets/models/**` | `gen_stages.py`, `*.lua` |
| Lua | `assets/components/*.lua` | `source/**`, `assets/scenes/**` |
| レベル | `source/gen_stages.py`, `assets/scenes/**` | `assets/components/**` |

### 幾何の規約

- 部屋は内寸の面と面が **`GAP = 4.0m`** 離れて隣り合う。その 4m を台形の廊下が貫く。
- 口の位置 `off` は部屋の**単位座標**（縮尺前）。壁モデルは単位寸法で作り transform scale で縮尺を掛ける。
- 照明は位置と range を縮尺倍、強さはそのまま（エンジンの減衰は逆二乗ではない。k² だと白飛び）。
- 絶対寸法の物（柵・隙間・溝・敷居・出口）は縮尺を掛けない。
- 溝は部屋に 1 本。落ちると直前の廊下の出口へ戻る。

---

## 踏んだ罠（時間を溶かした順）

- ★★**エンジンの重力は 9.8 ではなく 14 m/s²**（`PhysicsSystem.cpp`）。`jumpSpeed` を 9.8 で
  計算すると跳べる高さが 0.9 → 0.63 になり、高い口に届かない。
- ★**空中でも WASD は効く**が、`physics:jump` を Lua の task から撃った実験では水平に進まなかった。
  跳ぶ距離は必ず **人と同じ入力（W+SPACE）で実測**すること。実測: 大きさ 1 で 1 跳び 2.10m / 滞空 0.62 秒。
- ★**大きさを変える時は床の高さを渡す**（`useBody(..., floorY)`）。敷居 0.9m の廊下の中で
  床 0 に置くと床下へ落ち、復帰→落下のループになる。
- ★**目の高さは「体の足元 + 1.7 × 見た目の縮尺」**で置く。廊下の中は体と見た目の縮尺が違う。
- ★★**シーンを開いた時点で存在しなかった entity は `scene:spawn` しても描画されない。**
  → 実行時に動かす物は全部シーン JSON に置いて床下（y=-200）へ隠す（Pilot / Body_*）。
- ★**`CharacterController` の寸法は実行時に変えられない。** → 大きさごとに `Body_0..4`。
- ★★**Play 開始直後に書いた transform はシーンの復元に黙って上書きされる。** → 体は 1 秒押し込む。
- ★**`motionType 0`（STATIC）の当たり判定は transform を書いても動かない。**
- ★**Blender のエクスポータが `tex/*.png` を上書きする** → temp へ出して gltf/bin だけ持ち帰る。
- **エンジンはテクスチャをパス単位で一生キャッシュする** → `dx12_reload_assets{force:true}`。
- **MCP の `key_down` は `isKeyDown` に効くが、Play 中は実時間で走り続ける**ので
  `step_frames` の数で距離は測れない。検証用に `saveNum("dbg_lockYaw",1)`（マウス無視）と
  `saveNum("dbg_size", 2)`（その場で大きさ変更）のフックを残してある。
- **1 灯しか無い小部屋で蛍光灯を明滅させない**（部屋ごと真っ暗になる瞬間が出る）。
- 開口の寸法は `blender_kit.py` と `gen_stages.py` の **OPENINGS 2箇所**にある。
- `build/game/` を作り直し忘れると、clone した人は古い版を遊ぶ。

## 未了

人に触らせる（合否: 第1面で門から出た瞬間に「でかくなった」と口に出すか / 第7面で「門に潜ればいい」に
自力で気づくか）。過去の設計は `docs/SCALE.md`（v3〜v4.3）/ `docs/GATE.md` / `docs/LEVELS.md` に残してある。
